# 腾讯云服务器部署完整指南

## 📋 目录
1. [备案流程](#备案流程)
2. [打包软件](#打包软件)
3. [服务器配置](#服务器配置)
4. [部署网站](#部署网站)
5. [配置域名和HTTPS](#配置域名和https)

---

## 🏢 第一步：备案流程（15-20天）

### 为什么要备案？
- 中国大陆的服务器必须备案才能绑定域名
- 不备案只能用IP访问，很不专业
- 备案是免费的，但需要时间

### 备案步骤

#### 1. 准备材料
- ✅ 身份证正反面照片
- ✅ 手机号（用于接收验证码）
- ✅ 邮箱
- ✅ 域名（需要先购买）
- ✅ 腾讯云账号

#### 2. 购买域名（如果还没有）
```
1. 登录腾讯云控制台
2. 进入"域名注册"
3. 搜索想要的域名（例如：damaihelper.com）
4. 选择后缀（.com 最好，.cn 也可以）
5. 购买（一般50-100元/年）
```

**域名建议：**
- damaihelper.com
- ticketrush.cn
- fairticket.cn

#### 3. 开始备案
```
1. 登录腾讯云控制台
2. 搜索"网站备案"
3. 点击"开始备案"
4. 选择"首次备案"
```

#### 4. 填写信息
**主体信息：**
- 备案类型：个人备案
- 姓名：你的真实姓名
- 证件类型：身份证
- 证件号码：你的身份证号
- 联系方式：手机号、邮箱

**网站信息：**
- 网站名称：不能包含"大麦"等商标词
  - ✅ 推荐：个人技术博客、开源工具分享
  - ❌ 禁止：大麦抢票、商业相关
- 网站域名：你购买的域名
- 服务内容：个人博客/工具分享
- 网站备注：开源软件下载站

**重要提示：**
- ⚠️ 不要在备案信息中提到"抢票"、"大麦"等敏感词
- ⚠️ 网站名称要中性，例如"个人技术分享"
- ⚠️ 所有信息必须真实

#### 5. 上传资料
- 身份证正面照片
- 身份证反面照片
- 核验单（腾讯云会提供模板，打印签字拍照）

#### 6. 视频核验
- 腾讯云会发送短信
- 按照提示进行视频核验（1-2分钟）
- 需要本人出镜，读一段话

#### 7. 等待审核
```
腾讯云初审：1-2个工作日
管局审核：10-20个工作日
```

**审核期间：**
- 保持手机畅通
- 可能会接到核实电话
- 如实回答即可

#### 8. 备案成功
- 会收到短信和邮件通知
- 获得备案号（例如：京ICP备12345678号）
- 可以正式使用域名了

---

## 📦 第二步：打包软件

### Mac版本打包

```bash
# 1. 构建前端
cd /Users/zhangyuxin/Desktop/DamaiHelper/frontend
npm run build

# 2. 安装Electron打包工具
cd ../electron
npm install electron-builder --save-dev

# 3. 打包Mac版本
npm run build

# 生成的文件在：electron/dist/DamaiHelper-1.0.0.dmg
```

### Windows版本打包（需要在Windows电脑上）

如果你只有Mac，可以：
1. 找朋友的Windows电脑
2. 或者使用虚拟机（Parallels Desktop）
3. 或者使用云打包服务

```bash
# 在Windows上
cd DamaiHelper/frontend
npm run build

cd ../electron
npm install
npm run build

# 生成：electron/dist/DamaiHelper-Setup-1.0.0.exe
```

### 打包后的文件
```
DamaiHelper-Mac-v1.0.0.dmg      (约50MB)
DamaiHelper-Windows-v1.0.0.exe  (约60MB)
```

---

## 🖥️ 第三步：服务器配置

### 1. 登录腾讯云服务器

```bash
# 在你的Mac终端
ssh root@你的服务器IP

# 例如：
ssh root@123.456.789.0

# 输入密码（腾讯云会发到你邮箱）
```

### 2. 安装Nginx（Web服务器）

```bash
# 更新系统
apt update && apt upgrade -y

# 安装Nginx
apt install nginx -y

# 启动Nginx
systemctl start nginx
systemctl enable nginx

# 检查状态
systemctl status nginx
```

### 3. 配置防火墙

```bash
# 开放80端口（HTTP）
ufw allow 80

# 开放443端口（HTTPS）
ufw allow 443

# 开放22端口（SSH）
ufw allow 22

# 启用防火墙
ufw enable
```

**同时在腾讯云控制台：**
```
1. 进入"轻量应用服务器"
2. 点击你的服务器
3. 进入"防火墙"
4. 添加规则：
   - 80端口（HTTP）
   - 443端口（HTTPS）
   - 22端口（SSH）
```

### 4. 测试Nginx

在浏览器访问：`http://你的服务器IP`

如果看到"Welcome to nginx"，说明成功了！

---

## 🌐 第四步：部署网站

### 1. 创建网站目录

```bash
# 创建网站根目录
mkdir -p /var/www/damaihelper

# 创建下载目录
mkdir -p /var/www/damaihelper/downloads
```

### 2. 上传网站文件

**在你的Mac上：**

```bash
# 进入网站目录
cd /Users/zhangyuxin/Desktop/DamaiHelper/website

# 上传网站文件到服务器
scp index.html root@你的服务器IP:/var/www/damaihelper/

# 上传打包好的软件
scp ../electron/dist/DamaiHelper-Mac-v1.0.0.dmg root@你的服务器IP:/var/www/damaihelper/downloads/

# 如果有Windows版本
scp DamaiHelper-Windows-v1.0.0.exe root@你的服务器IP:/var/www/damaihelper/downloads/
```

### 3. 配置Nginx

```bash
# 在服务器上，编辑Nginx配置
nano /etc/nginx/sites-available/damaihelper

# 粘贴以下内容：
```

```nginx
server {
    listen 80;
    server_name 你的域名;  # 例如：damaihelper.com
    
    root /var/www/damaihelper;
    index index.html;
    
    # 网站主页
    location / {
        try_files $uri $uri/ =404;
    }
    
    # 下载文件
    location /downloads/ {
        alias /var/www/damaihelper/downloads/;
        autoindex on;  # 显示文件列表
    }
    
    # 日志
    access_log /var/log/nginx/damaihelper_access.log;
    error_log /var/log/nginx/damaihelper_error.log;
}
```

```bash
# 保存文件（Ctrl+O，回车，Ctrl+X）

# 创建软链接
ln -s /etc/nginx/sites-available/damaihelper /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启Nginx
systemctl restart nginx
```

### 4. 设置文件权限

```bash
# 设置所有者
chown -R www-data:www-data /var/www/damaihelper

# 设置权限
chmod -R 755 /var/www/damaihelper
```

---

## 🔒 第五步：配置域名和HTTPS

### 1. 域名解析

**在腾讯云控制台：**
```
1. 进入"域名管理"
2. 点击你的域名
3. 点击"解析"
4. 添加记录：
   - 记录类型：A
   - 主机记录：@（代表根域名）
   - 记录值：你的服务器IP
   - TTL：600
5. 再添加一条：
   - 记录类型：A
   - 主机记录：www
   - 记录值：你的服务器IP
   - TTL：600
```

**等待生效（5-10分钟）**

测试：`ping 你的域名`

### 2. 安装SSL证书（免费HTTPS）

```bash
# 安装Certbot
apt install certbot python3-certbot-nginx -y

# 自动配置SSL
certbot --nginx -d 你的域名 -d www.你的域名

# 例如：
certbot --nginx -d damaihelper.com -d www.damaihelper.com

# 按提示操作：
# 1. 输入邮箱
# 2. 同意服务条款（Y）
# 3. 是否接收邮件（N）
# 4. 选择重定向HTTP到HTTPS（2）
```

### 3. 自动续期

```bash
# SSL证书90天过期，设置自动续期
certbot renew --dry-run

# 添加定时任务
crontab -e

# 添加这一行（每天凌晨2点检查续期）
0 2 * * * certbot renew --quiet
```

### 4. 测试HTTPS

访问：`https://你的域名`

应该看到：
- ✅ 绿色小锁图标
- ✅ 网站正常显示
- ✅ 可以下载软件

---

## 📊 完整流程时间表

```
第1天：购买域名，提交备案
第2-3天：腾讯云初审
第4-20天：管局审核（等待）
第21天：备案通过
第21天：配置服务器（2小时）
第21天：部署网站（1小时）
第21天：配置域名和HTTPS（30分钟）
第21天：网站上线！🎉
```

---

## 🎯 快速命令汇总

### 一键部署脚本

在服务器上创建并运行：

```bash
# 创建部署脚本
nano deploy.sh
```

```bash
#!/bin/bash

echo "开始部署DamaiHelper网站..."

# 安装Nginx
apt update
apt install nginx -y

# 创建目录
mkdir -p /var/www/damaihelper/downloads

# 配置Nginx
cat > /etc/nginx/sites-available/damaihelper << 'EOF'
server {
    listen 80;
    server_name 你的域名;
    
    root /var/www/damaihelper;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
    
    location /downloads/ {
        alias /var/www/damaihelper/downloads/;
        autoindex on;
    }
}
EOF

# 启用站点
ln -s /etc/nginx/sites-available/damaihelper /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

# 设置权限
chown -R www-data:www-data /var/www/damaihelper
chmod -R 755 /var/www/damaihelper

echo "部署完成！请上传网站文件到 /var/www/damaihelper/"
```

```bash
# 运行脚本
chmod +x deploy.sh
./deploy.sh
```

---

## ⚠️ 重要注意事项

### 备案相关
1. **网站内容要合规**
   - 不要在网站上明显提到"抢票"功能
   - 可以说"演出信息提醒工具"
   - 强调"学习研究用途"

2. **备案信息要一致**
   - 网站实际内容要和备案信息一致
   - 不要放违规内容
   - 定期检查网站

3. **备案号要显示**
   - 在网站底部显示备案号
   - 链接到工信部网站

### 法律风险
1. **免责声明必须明显**
2. **不要宣传"100%成功"**
3. **不要收费**
4. **强调开源学习性质**

---

## 🆘 常见问题

### Q1: 备案被拒绝怎么办？
- 查看拒绝原因
- 修改后重新提交
- 常见原因：网站名称不合规、信息不完整

### Q2: 域名解析不生效？
- 等待5-10分钟
- 清除DNS缓存：`sudo dscacheutil -flushcache`
- 使用 `nslookup 你的域名` 检查

### Q3: 网站打不开？
```bash
# 检查Nginx状态
systemctl status nginx

# 查看错误日志
tail -f /var/log/nginx/error.log

# 检查防火墙
ufw status
```

### Q4: HTTPS证书申请失败？
- 确认域名已解析到服务器
- 确认80端口开放
- 检查Nginx配置是否正确

### Q5: 下载速度慢？
- 考虑使用CDN加速
- 腾讯云有免费的CDN服务
- 或者使用对象存储（COS）

---

## 📝 检查清单

部署前检查：
- [ ] 域名已购买
- [ ] 备案已通过
- [ ] 服务器已购买
- [ ] 软件已打包
- [ ] 网站文件已准备

部署后检查：
- [ ] 网站可以访问
- [ ] HTTPS正常
- [ ] 软件可以下载
- [ ] 手机端显示正常
- [ ] 备案号已显示

---

## 🎉 完成！

如果一切顺利，你现在应该有：
- ✅ 一个漂亮的官网
- ✅ HTTPS加密
- ✅ 可以下载软件
- ✅ 合法备案

**网站地址：** `https://你的域名`

恭喜！🎊

